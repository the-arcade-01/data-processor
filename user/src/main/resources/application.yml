server:
  port: 8085

spring:
  application:
    name: USER-MICROSERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8090/eureka/
  instance:
    hostname: localhost