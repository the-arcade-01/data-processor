server:
  port: 8080

spring:
  application:
    name: DEPARTMENT-MICROSERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8090/eureka/
  instance:
    hostname: localhost